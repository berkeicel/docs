---
title: "RampKit.getActiveSession()"
description: "Returns session metadata for debugging and analytics"
---

```ts
const session = RampKit.getActiveSession();
console.log(session.sessionId, session.flowId, session.timestamp);
```
