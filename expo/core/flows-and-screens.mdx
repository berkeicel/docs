---
title: "Flows & Screens"
description: "Flows are built visually in the RampKit web editor"
---

A flow is a sequence of screens built visually in the RampKit web editor. Each screen can include text, images, buttons, and behaviors that respond to user actions.

You’ll identify each flow by its Flow ID (e.g. `onboarding_main`).
