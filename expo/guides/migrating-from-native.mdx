---
title: "Migrating from Native Onboardings"
description: "Replace static native code with live flows"
---

Replace static native code with live flows. Simply remove old screens and call `RampKit.loadFlow()` where onboarding starts.
