---
title: "Expo Setup"
description: "Works in both managed and bare workflows"
---

RampKit is fully compatible with Expo (no native modules). If you eject, continue using the same package; just run `npx pod-install` when needed.
