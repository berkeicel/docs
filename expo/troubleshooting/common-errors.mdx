---
title: "Common Errors"
description: "Fix frequent setup or rendering issues fast"
---

- "Flow not found" → Check your Flow ID and publish status.
- "Bridge disconnected" → Ensure `RampKit.init()` runs before `loadFlow()`.
