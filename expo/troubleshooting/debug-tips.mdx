---
title: "Debug Tips"
description: "Best practices for inspecting network and bridge issues"
---

Enable verbose logging:

```ts
RampKit.enableDebug(true);
```

If issues persist, contact support with logs from `RampKit.getSessionId()`.
