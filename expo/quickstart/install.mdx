---
title: "Install"
description: "Add RampKit to your Expo project"
---

```bash
npx expo install rampkit-expo-dev react-native-webview react-native-pager-view react-native-root-siblings
```

For bare React Native, also run:

```bash
npx pod-install
```
