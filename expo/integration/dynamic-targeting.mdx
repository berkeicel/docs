---
title: "Dynamic Targeting"
description: "Load flows conditionally based on user state"
---

Load specific flows based on user segments.

```ts
const flow = user.isPremium ? "premium_intro" : "free_intro";
RampKit.loadFlow(flow);
```
