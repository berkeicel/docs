---
title: "Lifecycle Management"
description: "Reset sessions and re-attribute users after auth changes"
---

If your app includes authentication or user switching, call:

```ts
RampKit.reset();
RampKit.setUserAttributes(newUser);
RampKit.loadFlow("welcome_back");
```

This ensures a fresh session context per user.
